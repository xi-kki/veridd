name: VERIDD Agent Bot

on:
  schedule:
    # Every 60 minutes — 24 on-chain tx/day, stays within free tier (2,000 min/mo)
    - cron: '0 * * * *'
  workflow_dispatch:  # Manual trigger from GitHub UI

jobs:
  run-bot:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: runner

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run bot cycle
        run: node bot-once.js
        env:
          PRIVATE_KEY: ${{ secrets.BOT_PRIVATE_KEY }}
          GROQ_KEY: ${{ secrets.GROQ_KEY }}
          CONTRACT: ${{ secrets.CONTRACT || '0x70c88e1A57917409fdA2935F16A38deb4aEF5Bfa' }}
          NAME: Veridd-Bot-Alpha
